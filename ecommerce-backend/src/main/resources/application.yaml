spring:
  application:
    name: ecommerce-backend
  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    url: jdbc:h2:mem:test

#  jpa:
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true

#    this property helps to determine what happens to
#    tables in database every time web is run.(default create-drop)
#    helpful when not using in-memory databases
#    hibernate:
#      ddl-auto: update



